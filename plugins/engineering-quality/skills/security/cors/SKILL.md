---
name: cors
description: Use when configuring cross-origin access for an API.
---

# CORS

- Explicit allowed origins — no wildcard origin on an authenticated app.
- Review credentials and CORS configuration together, not separately.

Related skills in this plugin: `csrf`.
